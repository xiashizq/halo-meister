# Campaign Evolved tag definitions

The `haloce_evolved` JSON schemas are the Campaign Evolved definition set used
by Baboon. Halo Meister bundles the schemas so runtime tools work from an
extracted release without a separate Baboon installation.

Developers can override the bundled directory by setting
`HALOMEISTER_DEFINITIONS` to another compatible `haloce_evolved` definition
folder.
